# GTM Container Exports

Versioned exports of container `GTM-NHRCHKWS` (Admin → Export Container).
One export per published version, committed after each publish:

- `container-export.json` — latest published version.

The container configuration is treated as code: it lives in version control
alongside the site that hosts it.
